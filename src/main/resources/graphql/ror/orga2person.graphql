query orga2person($ror: ID!, $after: String) {
    organization(id: $ror) {
        id
        name

        people(after: $after) {
            pageInfo {
                endCursor
                hasNextPage
            }
            nodes {
                id
                givenName
                familyName
            }
        }
    }
}
