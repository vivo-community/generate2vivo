query orga2person($ror: ID!) {
    organization(id: $ror) {
        id
        name
        country {
            name
        }

        people {
            nodes {
                id
                givenName
                familyName
            }
        }
    }
}
