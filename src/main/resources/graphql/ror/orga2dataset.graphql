query orga2dataset($ror: ID!) {
    organization(id: $ror) {
        id
        name
        country {
            id
            name
        }

        datasets {
            nodes {
                titles {
                    title
                }
                types {
                    bibtex
                }
                doi
                publicationYear
            }
        }
    }
}
